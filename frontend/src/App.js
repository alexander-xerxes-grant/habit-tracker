import HabitContainer from './HabitContainer';

function App() {
  // ESLint will now properly recognize that HabitContainer is being used
  return (
    <div className="App">
      <HabitContainer />
    </div>
  );
}

export default App;